{
  "name": "real-time-incident-room",
  "version": "1.0.0",
  "description": "Real-Time AI Incident Room",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\" --names \"SERVER,CLIENT\" --prefix-colors \"cyan,magenta\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
